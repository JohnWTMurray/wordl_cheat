import data from "./data";
import words from "./words";


function oof(cells: Icell[]): Array<string> {

	let letters: string = "";

	cells.map(cell => {
		if (cell.status == 'amber') {
			return cell.
		}
	});

	return [];
}



export default function calculate(): Array<string> {


	data.game.forEach(g => {

		oof(g);
	})


	return [];
}
